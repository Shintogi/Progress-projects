Steps to ensure that code runs properly Make sure to download any recent version of python. Libraries used : dotenv, requests
----------------------------------------------------------------
install commands :
1.pip install python-dotenv 
 2.pip install requests 